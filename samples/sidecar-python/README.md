# sidecar-python

Python sidecar boilerplate using the structured `connectors` format.

## What it shows

- JSON-RPC over stdio
- filesystem and shell helpers in Python
- a clean split between browser UI and data/runtime code
