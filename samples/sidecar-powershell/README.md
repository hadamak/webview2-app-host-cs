# sidecar-powershell

PowerShell sidecar boilerplate using the structured `connectors` format.

## What it shows

- Windows-native automation without adding another runtime
- JSON-RPC sidecar communication
- a baseline for system administration tools and local task runners
