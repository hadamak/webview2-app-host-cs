# sidecar-node

Node.js sidecar boilerplate using the structured `connectors` format.

## What it shows

- JSON-RPC over stdio
- file and terminal helpers implemented in Node.js
- bridge calls from `host.js`

Place this folder as `www/` next to the built host, and ensure `node` is installed or bundled.
